# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — it contains only an IntelliJ `.idea/` workspace directory and no source code, build configuration, or documentation.

Intended purpose (inferred from the directory name): a financial tracker application. The stack, language, and architecture have not yet been chosen.

## What to do on first real change

When code is added to this repository, re-run `/init` so this file can be regenerated from actual sources. At that point, document:

- Build, lint, test, and run commands (including how to run a single test).
- High-level architecture that spans multiple files (data flow, module boundaries, key abstractions).
- Any conventions enforced by tooling that aren't obvious from the code.
